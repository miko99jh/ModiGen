Exemplary Test Case Prompt:
% \textbf{Modelica model:}

% within IndustrialControlSystems.LinearSystems.Continuous;

% model LeadLag "Lead lag process: mu(1+T1*s)/(1+T2*s)"

% \quad extends Interfaces.BaseBlock;
  
% \quad  parameter Real T1 = 3 "|Block parameters| zero's time constant";
  
% \quad  parameter Real T2 = 5 "|Block parameters| pole's time constant";
  
% \quad  parameter Real mu = 1 "|Block parameters| Gain";
  
% \quad  parameter Real y\_start = 0 "|Initial conditions| output initial value";
  
% \quad  Real dy;
  
% protected

% \quad  parameter Real A = T1/T2;
  
% \quad  parameter Real B = 1 - A;
  
% initial equation

% \quad  dy = y\_start;
  
% equation

% \quad  assert(abs(A-1)>1e-8,"T1 must be != T2");
  
% \quad  dy + T2*der(dy) = mu*B*u;
  
% \quad  y = u*mu*A + dy;
  
% end LeadLag;


% \textbf{Model description:}

% Continuous time transfer function of a lead lag process.

%    Y(s)/U(s) = mu*(1 + s*T1)/(1 + s*T2)
   
% The time constant T1 must be different from T2.

% \textbf{Prompt:}

% Use this model to solve the transfer function of the lead lag process, where the zero's time constant 'T1' of the lead lag process is 2, pole's time constant 'T2' is 3, the gain 'mu' is 2, and the initial conditions 'y\_start' is 0.

% The input signal 'stepSignal' is implemented by Modelica.Bl-ocks.Sources.Step, stepSignal is set to height=2, startTime=10.

% The connection of the signal is based on 'connect'. Please help me generate this modelica model, you don't need to provide "within".